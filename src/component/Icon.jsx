

// eslint-disable-next-line react/prop-types
const IconComponent = ({ Icon, color, size,onClick,value }) => {
  return <Icon color={color} size={size} onClick={onClick}  />;
};

export default IconComponent;
